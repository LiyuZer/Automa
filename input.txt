graph main {
    memory {
        n: 5.5
    }

    nodeDef {
        Start :: startNode;
        End :: acceptNode;
    }
    transitions {
        Start -> End => conditions : (), operations : (n = n + 0.1255); 
    }

}
 