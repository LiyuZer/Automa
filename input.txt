def main (c : 0):
    nodes :
        start :: ST;
        j :: AC;
        hey;
        once;
        moon;
 
    transitions:
        start -> moon => ((a - b / (c + 1)) > (d * (e + f)) && (g % 3 == 0 || h != i && j <= k)):
            a = 5 + 6;
        moon -> hey => ((a - b / (c + x1)) > (d * (e + f)) && (g % 3 == 0 || h != i && j <= k)):
            a = 5 + 6;
    accept:
    return 5;
