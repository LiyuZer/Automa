
graph main {
    memory{
        i : 0,
        num : 1
    }
    nodeDef{
        start :: startNode;
        accept :: acceptNode;
    }
    transitions{
        start -> start => conditions : (i) , operations : ( i, num);
        start -> accept => conditions : (i) , operations : (); 
    }
    afterAccept{
        num;
    }
}
