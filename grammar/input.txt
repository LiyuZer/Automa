
graph main {
    memory{
        Girum : 0,
        Tigist : 1
    }
    nodeDef{
        start :: startNode;
        accept :: acceptNode;
    }
    transitions{
        start -> start => conditions : (i) , operations : ( i, num);
        start -> accept => conditions : (i) , operations : (); 
    }
    afterAccept{
        num;
    }
}
