
graph main {
    memory{
        vim : 53,
        wassup : "This actually works what the hell"
    }
    nodeDef{
        start :: startNode;
        end :: rejectNode;
        accept :: acceptNode;
    }
    transitions{
        start -> end => conditions : ('0', 5, 2,3) , operations : ("hello1", true);
    }
}
