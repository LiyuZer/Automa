graph basic {
    memory {
        input: 5  // Global input string for DFA
    }
    nodeDef{
    start :: startNode;
    accept :: acceptNode;
    }
    transitions {
        Start Babgraph => conditions: () , operations: (input = input + 1); // Start `bab_graph` validation
    }
}

